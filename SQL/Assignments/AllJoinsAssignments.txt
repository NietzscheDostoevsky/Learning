1. 

SELECT 
	ENAME,
	JOB,
	SAL*12 AS ANNUALSAL,
	EMP.DEPTNO,
	DEPT.DNAME
FROM EMP INNER JOIN DEPT 
ON EMP.DEPTNO = DEPT.DEPTNO
WHERE EMP.SAL*12>30000 AND EMP.JOB !='CLERK'

2. 

SELECT E.ENAME, E.EMPNO, M.ENAME, M.EMPNO
FROM EMP E INNER JOIN EMP M 
ON E.MGR = M.EMPNO 

3. 

SELECT E.ENAME , D.DNAME
FROM DEPT D LEFT JOIN EMP E
ON E.DEPTNO = D.DEPTNO

4. 

SELECT D.DNAME, SUM(SAL) DEPTSAL
FROM EMP E LEFT  JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO 
GROUP BY D.DNAME


5. 

SELECT ENAME, DNAME 
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO

6. 

SELECT ENAME, LOC 
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO 
WHERE E.COMM IS NOT NULL

7. 

SELECT ENAME, DNAME 
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO 
WHERE E.SAL IN ( SELECT MIN(SAL) FROM EMP) AND
		MGR IS NULL 

SELECT ENAME, DNAME 
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO 
WHERE E.SAL IN ( SELECT MAX(SAL) FROM EMP) AND
		MGR IS NULL 


8. 

SELECT ENAME, DNAME , LOC
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO 
WHERE E.MGR = (SELECT EMPNO FROM EMP WHERE ENAME = 'SMITH')

9. 

SELECT ENAME, DNAME , LOC
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO 
WHERE E.ENAME IN (SELECT ENAME 
				 FROM EMP 
				 WHERE EMPNO IN (SELECT MGR 
				 			 FROM EMP 
				 			 WHERE EMPNO IN (SELECT MGR 
				 			 			FROM EMP 
				 			 			WHERE JOB = 'SALESMAN')) )

10. 

SELECT E.ENAME, D.DNAME, E.JOB 
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO 
WHERE D.DNAME = 'RESEARCH' AND 
E.JOB = 'MANAGER'

11. 

SELECT COUNT(ENAME)
FROM EMP 
WHERE SAL < (SELECT SAL FROM EMP 
			 WHERE EMPNO = (SELECT MGR 
			 			   FROM EMP 
			 			   WHERE ENAME = 'BLAKE'))

12. 

SELECT E.ENAME, D.DNAME , D.LOC
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO
WHERE E.JOB = 'ANALYST' AND 
E.MGR = (SELECT EMPNO FROM EMP WHERE ENAME = 'BLAKE')

13. 

SELECT ENAME, HIREDATE, COMM 
FROM EMP
WHERE EMPNO = (SELECT MGR 
				FROM EMP
				WHERE ENAME = 'FORD')

14. 

SELECT E.ENAME, D.DNAME 
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO
WHERE SAL < (SELECT AVG(SAL) FROM EMP WHERE DEPTNO=30)

15.  *

SELECT E.ENAME, D.DNAME 
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO
WHERE MGR = (SELECT EMPNO FROM EMP WHERE ENAME='JONES')

16.

SELECT E.ENAME, D.DNAME 
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO
WHERE E.ENAME LIKE 'S%'

17. 

SELECT D.DNAME 
FROM DEPT D LEFT JOIN EMP E 
ON D.DEPTNO = E.DEPTNO
WHERE E.ENAME IS NULL

SELECT D.DNAME 
FROM EMP E RIGHT OUTER JOIN DEPT D
ON D.DEPTNO = E.DEPTNO
WHERE E.ENAME IS NULL

18.  *

SELECT E1.EMPNO, E1.ENAME, E1.SAL , E2.ENAME , E2.SAL
FROM EMP E1 JOIN EMP E2 
ON E1.SAL = E2.SAL 
WHERE E1.EMPNO != E2.EMPNO 

SELECT E1.EMPNO, E1.ENAME, E1.COMM , E2.ENAME , E2.COMM
FROM EMP E1 JOIN EMP E2 
ON E1.COMM = E2.COMM 
WHERE E1.EMPNO != E2.EMPNO 

ChatGPT answer
SELECT EMPNO, ENAME, SAL
FROM EMP
WHERE SAL IN ( SELECT SAL 
			   FROM EMP 
			   GROUP BY SAL 
			   HAVING COUNT(*)>1)

19. * q : Why all names are getting selected. 

SELECT DNAME , ENAME
FROM DEPT D LEFT OUTER JOIN EMP E 
ON D.DEPTNO = E.DEPTNO 

20.  

SELECT E.ENAME, E.JOB, D.DNAME , D.LOC
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO
WHERE E.JOB = 'MANAGER' AND D.LOC = 'CHICAGO'

21. 

SELECT E.ENAME, E.HIREDATE, E.SAL, D.DNAME
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO
WHERE HIREDATE BETWEEN '01-JAN-1981' AND '31-DEC-1982'
AND E.SAL > 1800

22. 

SELECT MIN(SAL)
FROM EMP
WHERE SAL > (SELECT MIN(SAL) FROM EMP )

23. 

SELECT ENAME, SAL*12 AS ANNUALSAL, HIREDATE
FROM EMP
WHERE SAL*12 > 1500 AND HIREDATE < '01-JAN-82'

24. **


SELECT E1.ENAME, D.DNAME, D.LOC
FROM EMP E1 JOIN EMP E2 
ON E1.MGR = E2.MGR 
LEFT JOIN DEPT D 
ON E1.DEPTNO = D.DEPTNO 
WHERE E1.EMPNO != E2.EMPNO

SELECT E1.DNAME, E1.LOC, E1.DEPTNO
FROM 
	( SELECT D1.DNAME, D1.LOC, D1.DEPTNO, E1.MGR
	  FROM EMP E1 JOIN DEPT D1 ON E1.DEPTNO=D1.DEPTNO) E1
	JOIN
	(SELECT E3.MGR, COUNT(*) AS NUM_EMPLOYESS
		FROM EMP E3
		GROUP BY E3.MGR 
		HAVING COUNT(*) > 1) E2 
	ON E1.MGR = E2.MGR


SELECT *
FROM 
	( SELECT D1.DNAME, D1.LOC, D1.DEPTNO, E1.MGR
	  FROM EMP E1 JOIN DEPT D1 ON E1.DEPTNO=D1.DEPTNO) E1
	JOIN
	(SELECT E3.MGR, COUNT(*) AS NUM_EMPLOYESS
		FROM EMP E3
		GROUP BY E3.MGR 
		HAVING COUNT(*) > 1) E2 
	ON E1.MGR = E2.MGR

alternate 

SELECT DNAME, LOC, DEPTNO 
FROM DEPT
WHERE DEPTNO IN ( SELECT DEPTNO
				  FROM EMP 
				  GROUP BY DEPTNO 
				  HAVING COUNT(MGR) > 1)


25. 

SELECT E1.ENAME EMPLOYEE, M1.ENAME MANAGER
FROM EMP E1 INNER JOIN EMP M1
ON E1.MGR = M1.EMPNO

26. 

SELECT E.ENAME, D.DNAME 
FROM EMP E LEFT JOIN DEPT D 
ON E.DEPTNO = D.DEPTNO
WHERE E.ENAME LIKE 'S%'

27.  

SELECT * 
FROM EMP E1 JOIN EMP E2 
ON E1.SAL = E2.SAL 
WHERE E1.EMPNO != E2.EMPNO

ans 2 

SELECT EMPNO, ENAME, SAL
FROM EMP
WHERE SAL IN ( SELECT SAL 
			   FROM EMP 
			   GROUP BY SAL 
			   HAVING COUNT(*)>1)


select ename
from emp
where sal in ( select sal 
				from emp
				group by sal
				having count(*) > 1)

select e1.ename
from emp e1 join emp e2
on e1.sal = e2.sal 
where e1.empno != e2.empno 

select ename 
from emp
where comm is not null 
and comm in ( select comm 
				from emp
				where  comm is not null 
				group by comm
				having count(*)>1 )
