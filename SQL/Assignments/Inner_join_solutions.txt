1. 

SELECT ENAME, DNAME 
FROM EMP INNER JOIN DEPT
ON EMP.DEPTNO = DEPT.DEPTNO

2. 

SELECT EMPNO, ENAME, JOB, LOC
FROM EMP INNER JOIN DEPT
ON EMP.DEPTNO = DEPT.DEPTNO

3. 

SELECT EMPNO, SAL, LOC
FROM EMP INNER JOIN DEPT
ON EMP.DEPTNO = DEPT.DEPTNO
WHERE JOB = 'SALESMAN'

4. 

