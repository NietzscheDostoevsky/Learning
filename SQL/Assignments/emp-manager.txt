1.  

SELECT ENAME, SAL, COMM 
FROM EMP
WHERE EMPNO = ( SELECT MGR
				FROM EMP
				WHERE ENAME = 'JONES')

2.  

SELECT ENAME, SAL, JOB
FROM EMP
WHERE MGR IN   (SELECT EMPNO 
				  FROM EMP
				 WHERE JOB = 'MANAGER')

3. 

SELECT ENAME 
FROM EMP
WHERE MGR IN ( SELECT EMPNO
			     FROM EMP
			    WHERE MGR = (SELECT EMPNO
			     			   FROM EMP
			     			  WHERE ENAME = 'KING')) 

4. 

SELECT ENAME
FROM EMP
WHERE MGR = ( SELECT MGR 
			   FROM EMP
			   WHERE ENAME = 'ALLEN')


5. 

SELECT ENAME 
FROM EMP
WHERE EMPNO NOT IN (SELECT MGR FROM EMP )

SELECT ENAME 
FROM EMP
WHERE EMPNO NOT IN (SELECT MGR FROM EMP WHERE MGR IS NOT NULL)


6. 

SELECT ENAME
FROM EMP
WHERE MGR IS NULL AND EMPNO IN (SELECT MGR FROM EMP)

7. 

SELECT ENAME
FROM EMP
WHERE EMPNO = ( SELECT MGR 
			  FROM EMP
			  WHERE EMPNO = (SELECT MGR 
			  				FROM EMP
							WHERE ENAME = 'SMITH'))


8. 

SELECT ENAME 
FROM EMP 
WHERE MGR IN ( SELECT EMPNO
				 FROM EMP 
				 WHERE MGR  IN (SELECT EMPNO
				 				FROM EMP
								WHERE ENAME = 'JONES') )

9. 

SELECT ENAME
FROM EMP
WHERE EMPNO IN ( SELECT MGR 
				FROM EMP
				WHERE JOB = 'SALESMAN')

10. 

SELECT ENAME 
FROM EMP 
WHERE EMPNO = ( SELECT MGR 
				FROM EMP 
				WHERE EMPNO = (SELECT MGR 
								FROM EMP
								WHERE EMPNO = (SELECT MGR
												FROM EMP
												WHERE ENAME = 'ADAMS')))

11. 

SELECT ENAME
FROM EMP
WHERE EMPNO IN ( SELECT MGR 
				 FROM EMP
				 WHERE MGR IS NOT NULL
				 GROUP BY MGR 
				 HAVING COUNT(ENAME) = 1  )


12. 

SELECT ENAME
FROM EMP
WHERE EMPNO IN ( SELECT MGR 
				 FROM EMP
				 WHERE MGR IS NOT NULL
				 GROUP BY MGR 
				 HAVING COUNT(ENAME) > 2  )

13. 

SELECT ENAME 
FROM EMP 
WHERE EMPNO IN ( SELECT MGR 
				 FROM EMP 
				 WHERE SAL < 2000)

14. 

SELECT ENAME, SAL, DEPTNO 
FROM EMP
WHERE MGR = (SELECT EMPNO 
			 FROM EMP
			 WHERE ENAME = 'BLAKE')

15. 

SELECT ENAME 
FROM EMP 
WHERE EMPNO IN ( SELECT MGR 
				FROM EMP 
				WHERE HIREDATE LIKE '%FEB%')